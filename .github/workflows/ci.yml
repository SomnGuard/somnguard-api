name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2

      - name: Generate test JWT keys
        run: |
          mkdir -p src/test/resources/keys/test
          openssl genrsa -out src/test/resources/keys/test/private.pem 2048
          openssl rsa -in src/test/resources/keys/test/private.pem -pubout -out src/test/resources/keys/test/public.pem

      - name: Install Maven dependencies
        run: mvn -B dependency:go-offline

      - name: Build with Maven
        run: mvn -B clean compile

      - name: Run tests
        run: mvn -B test

      - name: Checkstyle check
        run: mvn -B checkstyle:check

      - name: Dependency Check
        run: mvn -B org.owasp:dependency-check-maven:check -DfailBuildOnCVSS=7

      - name: Docker build
        run: |
          docker build -t somnguard-api:latest .
        env:
          DOCKER_HOST: unix:///var/run/docker.sock